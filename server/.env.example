PORT=4000
CLIENT_ORIGIN=http://localhost:5173
DB_PATH=./data/chatmamba.db
SESSION_NAME=chatmamba-default

# Auth — seeded on first boot if no users exist
ADMIN_EMAIL=luffyheng@gmail.com
ADMIN_PASSWORD=Testing123

# Used to build password-reset links
# Set to https://blast.ayadvisorysolution.com on the VPS
APP_ORIGIN=http://localhost:5173

# When NODE_ENV=production the session cookie is sent only over HTTPS
# NODE_ENV=production
